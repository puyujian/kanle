import { defineConfig, globalIgnores } from "eslint/config";
import nextVitals from "eslint-config-next/core-web-vitals";
import nextTs from "eslint-config-next/typescript";

const eslintConfig = defineConfig([
  ...nextVitals,
  ...nextTs,
  {
    rules: {
      // React Compiler is not enabled in next.config.ts. Keep the runtime Hook
      // correctness rules, but do not enforce compiler-only eligibility checks.
      "react-hooks/globals": "off",
      "react-hooks/immutability": "off",
      "react-hooks/preserve-manual-memoization": "off",
      "react-hooks/purity": "off",
      "react-hooks/refs": "off",
      "react-hooks/set-state-in-effect": "off",
      "react-hooks/static-components": "off",
      // Most occurrences are interactive media, object URLs, emoji, or editor
      // content where next/image cannot provide useful optimization.
      "@next/next/no-img-element": "off",
    },
  },
  {
    files: ["scripts/**/*.js"],
    rules: {
      // Deployment scripts run as CommonJS in this package.
      "@typescript-eslint/no-require-imports": "off",
    },
  },
  // Override default ignores of eslint-config-next.
  globalIgnores([
    // Default ignores of eslint-config-next:
    ".next/**",
    "out/**",
    "build/**",
    "next-env.d.ts",
  ]),
]);

export default eslintConfig;
